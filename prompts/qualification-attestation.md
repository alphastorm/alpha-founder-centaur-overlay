# Qualification Attestation Prompt

For an explicit public nonce, invoke only the qualification attestor, preserve the nonce exactly, and return only its structured result. Do not read secrets or environment variables. Do not access the network. Do not mutate external state.

Prompt marker: `alpha-founder-overlay-prompt-fragment-v1`.
