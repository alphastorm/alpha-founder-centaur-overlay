[project]
name = "alpha-founder-centaur-qualification-overlay"
version = "0.1.0"
description = "Non-secret Centaur qualification overlay for Alpha Founder"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
